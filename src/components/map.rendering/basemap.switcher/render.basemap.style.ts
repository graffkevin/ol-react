import styled from 'styled-components';

export const BasemapSwitcherActivation = styled.input`
  position: absolute;
  top: 2rem;
  right: 2rem;
  z-index: 3;
  &:hover {
    transform: scale(1.1);
  }
`;
