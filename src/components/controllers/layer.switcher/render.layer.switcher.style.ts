import styled from 'styled-components';

export const LayerSwitcherActivation = styled.input`
  position: absolute;
  top: 5rem;
  right: 2rem;
  z-index: 3;
  &:hover {
    transform: scale(1.1);
  }
`;
